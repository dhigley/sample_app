<!-- Exercise 10: Refactor the Home page to use separate partials for the two branches of the if-else statement. -->
<% if signed_in? %>
  <%= render 'shared/user_homepage' %>
<!-- Listing 10.28: Serves up different HTML based on whether the site visitor is signed in or not-->
<% else %>
  <%= render 'shared/homepage' %>
<% end %>
