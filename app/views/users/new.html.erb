<!-- Listing 5.36: Signup page stub. -->
<% provide(:title, 'Sign up') %>
<h1><%= yield(:title) %></h1>

<!-- Listing 7.17: A form to sign up new users. -->
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <!-- 7.23: Code to display error messages on the signup form. -->
      <%= render 'fields', f: f %>
      <%= f.submit "Create my account", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
